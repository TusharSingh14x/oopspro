# =====================================
# 🚀 Server Configuration
# =====================================
server.port=8080

# =====================================
# 🐘 PostgreSQL (Neon) Database Connection
# =====================================
spring.datasource.url=jdbc:postgresql://ep-proud-bush-a4nt5ai1.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_bQeOHsVn4qu8
spring.datasource.driver-class-name=org.postgresql.Driver

# =====================================
# 🧱 JPA (Hibernate) Configuration
# =====================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# =====================================
# 🌐 CORS (React Frontend)
# Allow all localhost ports during local dev
# =====================================
spring.web.cors.allowed-origin-patterns=http://localhost:*,http://127.0.0.1:*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
